/** @type {import('next').NextConfig} */
const nextConfig = {
  // Runs as a real server app on Vercel (API routes need a runtime, not a
  // static export), so no `output: "export"` here.
};

export default nextConfig;
