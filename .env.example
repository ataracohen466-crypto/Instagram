# Copy to .env.local for local dev, or set in Vercel → Settings → Environment Variables.
# Without this the app still runs, using canned fallback responses instead of Claude.
ANTHROPIC_API_KEY=sk-ant-...
